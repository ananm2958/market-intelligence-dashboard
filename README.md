# Market Intelligence Dashboard

React/Vite dashboard (`frontend`) with an Express, Prisma, PostgreSQL, and Redis API (`backend`).
Start local services with `docker compose up -d`, then configure the backend, migrate Prisma, and run each package's `dev` script.
