import type { HistoryBar } from '../lib/types';
export function StockChart({ data, symbol }: { data: HistoryBar[]; symbol: string }) { const values = data.map((bar) => bar.close); const min = Math.min(...values); const max = Math.max(...values); const points = data.map((bar, i) => `${(i / Math.max(data.length - 1, 1)) * 100},${100 - ((bar.close - min) / Math.max(max - min, 1)) * 100}`).join(' '); return <section className="market-card chart-card"><h2>{symbol} · 30-day trend</h2><svg viewBox="0 0 100 100" preserveAspectRatio="none" aria-label={`${symbol} price chart`}><polyline points={points} fill="none" stroke="currentColor" strokeWidth="2" vectorEffect="non-scaling-stroke" /></svg></section>; }
